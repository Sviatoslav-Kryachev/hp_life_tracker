# HP Life Tracker

Приложение для отслеживания активности и опыта (XP). Позволяет создавать активности, запускать таймеры, зарабатывать XP и тратить его на награды. Включает систему streak, умные рекомендации и черный список наград.

> 📝 **История изменений**: Все изменения и обновления проекта документируются в [CHANGELOG.md](./docs/CHANGELOG.md)

## 🚀 Возможности

### Основные функции
- 🎯 **Создание и управление активностями** - создавайте активности с настраиваемой ставкой XP/час
- ⏱️ **Таймер для отслеживания времени** - точный подсчет XP даже для коротких сессий
- 💰 **Система XP и уровней** - зарабатывайте XP и повышайте уровень (каждые 1000 XP)
- 🏆 **Награды за накопленный XP** - тратьте XP на награды (McDonald's, YouTube, игры и т.д.)
- 📊 **Ручной ввод времени** - добавление времени активности вручную
- 📈 **Статистика** - виджеты "Сегодня", "Неделя", история транзакций
- 📅 **Интерактивный календарь** - визуальный календарь с фильтрами (неделя/месяц/год) и отметками активных дней
- 🔍 **Детали дня** - клик по дню показывает полную информацию о заработках и расходах

### Новые функции
- 🔥 **Система Streak (серии дней)** - как в Duolingo:
  - Отслеживание дней подряд активности
  - Автоматические бонусы XP:
    - 2 XP за каждый день подряд (базовый)
    - +10 XP за 3 дня подряд
    - +20 XP за неделю подряд
    - +100 XP за месяц без пропусков
  - Виджет отображения текущей серии и рекорда

- 💡 **Умные рекомендации**:
  - "Продолжить серию" - если вчера делали активность
  - "Давно не делали" - напоминание о забытых активностях (4+ дней)
  - "Можно ещё" - предложение продолжить активность сегодня
  - Приоритеты: высокий/средний/низкий
  - Кнопка "Начать" прямо из рекомендации

- 🚫 **Черный список наград**:
  - Блокировка доступа к наградам без достаточного XP
  - Автоматическая проверка при покупке
  - Сообщения о блокировке с указанием требуемого XP
  - Управление через API

- 🎯 **Система целей**:
  - Создание целей с целевым количеством XP
  - Автоматическое отслеживание прогресса
  - Привязка к активности (опционально) - учитывается только XP от этой активности
  - Установка дедлайна (опционально)
  - Уведомления при достижении цели
  - Красивые карточки с прогресс-барами
  - Просмотр целей в админ-панели

- 🤖 **Telegram-бот**:
  - Быстрое занесение времени через команды
  - Получение отчётов и статистики
  - Команда `/xp` - показывает баланс XP и уровень
  - Команда `/add_time <минуты> <активность>` - добавить время
  - Команда `/report` - отчёт за сегодня
  - Команда `/activities` - список активностей
  - Автоматическое начисление XP и бонусов streak

### Аутентификация
- 🔐 Регистрация и вход пользователей
- 🔑 JWT токены для безопасной авторизации
- 👤 Персональные данные и статистика

## 📦 Установка

```bash
# Клонируйте репозиторий
git clone <repo-url>
cd hp_life_tracker

# Создайте виртуальное окружение
python -m venv venv

# Активируйте виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Создайте таблицы в базе данных
python -c "from app.utils.database import Base, engine; from app.models.base import *; Base.metadata.create_all(bind=engine)"
```

## 🏃 Запуск

### Основное приложение

```bash
# Запуск сервера
uvicorn app.main:app --reload --host 127.0.0.1 --port 8003

# Откройте в браузере
# http://127.0.0.1:8003/static/index.html
```

### Telegram-бот

1. **Получите токен бота:**
   - Найдите @BotFather в Telegram
   - Отправьте `/newbot`
   - Следуйте инструкциям и скопируйте токен

2. **Установите переменную окружения:**
   ```bash
   # Windows
   set TELEGRAM_BOT_TOKEN=your_token_here
   
   # Linux/Mac
   export TELEGRAM_BOT_TOKEN=your_token_here
   ```

3. **Запустите бота:**
   ```bash
   python run_telegram_bot.py
   ```

4. **Привяжите Telegram аккаунт:**
   - Войдите в веб-приложение
   - Используйте API endpoint `/telegram/link` для привязки
   - Или добавьте UI для привязки в настройках

## 📡 API Endpoints

### Аутентификация
- `POST /auth/register` - Регистрация нового пользователя
- `POST /auth/login` - Вход в систему (возвращает JWT токен)
- `GET /auth/me` - Получить информацию о текущем пользователе

### Активности
- `GET /activities/` - Список активностей пользователя
- `POST /activities/` - Создать активность
- `PUT /activities/{id}` - Обновить активность
- `DELETE /activities/{id}` - Удалить активность

### Таймер
- `POST /timer/start?activity_id={id}` - Запустить таймер для активности
- `POST /timer/stop/{log_id}` - Остановить таймер (возвращает XP и streak бонус)
- `POST /timer/manual` - Добавить время вручную
- `GET /timer/active` - Получить активные таймеры

### XP и кошелёк
- `GET /xp/wallet` - Получить баланс XP, уровень, статистику
- `GET /xp/today` - Статистика за сегодня (заработано, потрачено, сессии, время)
- `GET /xp/week` - Статистика за неделю (по дням)
- `GET /xp/month` - Статистика за месяц (по дням)
- `GET /xp/year` - Статистика за год (по месяцам)
- `GET /xp/day/{date}` - Детальная информация по конкретному дню (YYYY-MM-DD)
- `GET /xp/history` - История заработанного XP
- `GET /xp/full-history` - Полная история доходов и расходов

### Награды
- `GET /rewards/` - Список наград (пользовательские + общие)
- `POST /rewards/` - Создать награду
- `PUT /rewards/{id}` - Обновить награду
- `DELETE /rewards/{id}` - Удалить награду
- `POST /rewards/spend/{id}` - Потратить XP на награду (проверяет черный список)
- `GET /rewards/purchases` - История покупок наград

### Streak (серии дней)
- `GET /streak/` - Получить текущий streak (серия дней, рекорд)
- `POST /streak/update` - Обновить streak (вызывается автоматически после активности)

### Рекомендации
- `GET /recommendations/` - Получить умные рекомендации на основе истории активности

### Черный список
- `GET /blacklist/` - Получить список заблокированных наград
- `POST /blacklist/` - Добавить награду в черный список
- `DELETE /blacklist/{id}` - Удалить из черного списка
- `POST /blacklist/check/{reward_id}` - Проверить, заблокирована ли награда

### Цели
- `GET /goals/` - Получить все цели пользователя
- `POST /goals/` - Создать новую цель
- `PUT /goals/{id}` - Обновить цель
- `DELETE /goals/{id}` - Удалить цель
- `POST /goals/{id}/complete` - Отметить цель как выполненную

### Telegram
- `POST /telegram/link` - Привязать Telegram аккаунт
- `POST /telegram/unlink` - Отвязать Telegram аккаунт
- `GET /telegram/status` - Проверить статус привязки

## 🔥 Система Streak

Система автоматически отслеживает дни подряд активности и начисляет бонусы:

- **День 1-2**: 2 XP за каждый день
- **День 3**: +10 XP бонус (итого 16 XP)
- **День 7**: +20 XP бонус (итого 34 XP)
- **День 30**: +100 XP бонус (итого 160 XP)

Streak обновляется автоматически при завершении любой активности через таймер.

## 💡 Умные рекомендации

Алгоритм анализирует историю активности и предлагает:

1. **Продолжить серию** (высокий приоритет) - если вчера делали активность
2. **Давно не делали** (средний приоритет) - если 4+ дней не занимались
3. **Можно ещё** (низкий приоритет) - если сегодня мало времени потратили

Рекомендации обновляются автоматически и показывают топ-5 наиболее актуальных.

## 🚫 Черный список наград

Позволяет блокировать доступ к наградам без достаточного XP:

- Награды в черном списке требуют полной стоимости XP для доступа
- При попытке покупки без XP показывается сообщение о блокировке
- Можно добавить паттерны названий (например, "YouTube", "Instagram", "Игры")
- Управление через API `/blacklist/`

Пример использования:
```python
# Добавить YouTube в черный список
POST /blacklist/
{
  "reward_name_pattern": "YouTube",
  "is_active": true
}
```

## 🤖 Telegram-бот

Telegram-бот позволяет быстро заносить время и получать отчёты прямо из мессенджера.

### Команды бота:

- `/start` - Начать работу с ботом, показать инструкции
- `/xp` - Показать баланс XP, уровень и статистику
- `/add_time <минуты> <название активности>` - Добавить время активности
  - Пример: `/add_time 30 German` - добавить 30 минут для активности "German"
  - Название можно писать частично (например, "Ger" вместо "German")
- `/report` - Получить отчёт за сегодня (XP, время, топ активностей)
- `/activities` - Список всех ваших активностей
- `/help` - Справка по командам

### Настройка бота:

1. Получите токен у @BotFather
2. Установите переменную окружения `TELEGRAM_BOT_TOKEN`
3. Запустите `python run_telegram_bot.py`
4. Привяжите аккаунт через API `/telegram/link` или веб-интерфейс

### Примеры использования:

```
/xp
💰 Баланс: 150 XP
📊 Уровень: 1
📈 Всего заработано: 200 XP
💸 Всего потрачено: 50 XP

🔥 Сегодня: +30 XP
```

```
/add_time 45 Programming
✅ Время добавлено!

📝 Активность: Programming
⏱ Время: 45 минут
💰 Заработано: 15 XP
🔥 Бонус за серию: +10 XP
💳 Баланс: 165 XP
```

```
/report
📊 Отчёт за сегодня

💰 Заработано: 45 XP
⏱ Время: 75 минут
📝 Сессий: 3

Активности:
• Programming: 45м (15 XP)
• German: 30м (30 XP)
```

## 🎨 Интерфейс

### Виджеты на главной странице:
1. **Streak** - огненный виджет с текущей серией дней (структурированный дизайн)
2. **Сегодня** - статистика за сегодня (XP, сессии, время)
3. **Календарь** - интерактивный календарь с фильтрами:
   - **Неделя** - 7 дней с отметками активности
   - **Месяц** - полноценный календарь-сетка с отметками активных дней
   - **Год** - 12 месяцев с визуализацией активности
   - Клик по дню/месяцу показывает детальную информацию
4. **Прогресс** - прогресс до следующего уровня
5. **Рекомендации** - умные советы на основе истории (зелёный дизайн, кнопки запуска активности)
6. **История** - полная история транзакций

### Карточки наград:
- Иконка и название сверху
- Стоимость XP и кнопки управления снизу
- Автоматическое определение брендов (McDonald's, YouTube, Netflix и т.д.)
- Цветовая схема по брендам

## 🛠️ Технологии

- **Backend**: 
  - FastAPI - современный веб-фреймворк
  - SQLAlchemy - ORM для работы с БД
  - Pydantic - валидация данных
  - JWT (python-jose) - аутентификация
  - passlib + bcrypt - хеширование паролей

- **Frontend**: 
  - HTML5, Tailwind CSS - современный UI
  - Vanilla JavaScript - без фреймворков
  - Font Awesome - иконки

- **Database**: 
  - SQLite - легковесная БД (можно заменить на PostgreSQL)

## 📝 Структура проекта

```
hp_life_tracker/
├── app/
│   ├── main.py              # Точка входа FastAPI
│   ├── models/
│   │   └── base.py          # SQLAlchemy модели
│   ├── routers/
│   │   ├── auth.py          # Аутентификация
│   │   ├── activities.py    # Активности
│   │   ├── rewards.py       # Награды
│   │   ├── timer.py         # Таймер
│   │   ├── xp.py            # XP и статистика
│   │   ├── streak.py        # Система streak
│   │   ├── recommendations.py # Умные рекомендации
│   │   ├── blacklist.py     # Черный список
│   │   ├── goals.py         # Система целей
│   │   ├── telegram.py      # Telegram интеграция
│   │   └── admin.py         # Админ-панель
│   ├── schemas/
│   │   └── *.py             # Pydantic схемы
│   └── utils/
│       ├── database.py       # Настройка БД
│       └── auth.py           # Утилиты аутентификации
├── static/
│   ├── index.html           # Главная страница
│   ├── app.js               # Frontend логика
│   └── style.css            # Дополнительные стили
├── requirements.txt         # Зависимости
└── README.md               # Документация
```

## 🔒 Безопасность

- Пароли хешируются с помощью bcrypt
- JWT токены для авторизации
- Проверка прав доступа на всех эндпоинтах
- CORS настроен для разработки (измените для продакшена)

## 🚧 Разработка

Для разработки рекомендуется:
1. Использовать виртуальное окружение
2. Включить `--reload` для автоматической перезагрузки
3. Проверять логи сервера для отладки
4. Использовать инструменты разработчика браузера

## 📄 Лицензия

MIT License

## 👤 Автор

HP Life Tracker - система отслеживания активности и мотивации
