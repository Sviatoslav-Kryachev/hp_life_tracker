# HP Life Tracker

Приложение для отслеживания активности и опыта (XP). Позволяет создавать активности, запускать таймеры, зарабатывать XP и тратить его на награды.

## Возможности

- 🎯 Создание и управление активностями
- ⏱️ Таймер для отслеживания времени
- 💰 Система XP и уровней
- 🏆 Награды за накопленный XP
- 📊 Ручной ввод времени

## Установка

```bash
# Клонируйте репозиторий
git clone <repo-url>
cd hp_life_tracker

# Создайте виртуальное окружение
python -m venv venv

# Активируйте виртуальное окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

## Запуск

```bash
# Запуск сервера
uvicorn app.main:app --reload

# Откройте в браузере
# http://127.0.0.1:8000/static/index.html
```

## API Endpoints

- `GET /activities/` - Список активностей
- `POST /activities/` - Создать активность
- `PUT /activities/{id}` - Обновить активность
- `DELETE /activities/{id}` - Удалить активность
- `POST /timer/start` - Запустить таймер
- `POST /timer/stop/{log_id}` - Остановить таймер
- `POST /timer/manual` - Добавить время вручную
- `GET /xp/wallet` - Получить баланс XP
- `GET /rewards/` - Список наград
- `POST /rewards/spend/{id}` - Потратить XP на награду

## Технологии

- **Backend**: FastAPI, SQLAlchemy, Alembic
- **Frontend**: HTML, Tailwind CSS, JavaScript
- **Database**: SQLite
