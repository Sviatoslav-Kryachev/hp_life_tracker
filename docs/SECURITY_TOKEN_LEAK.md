# ⚠️ КРИТИЧЕСКАЯ ПРОБЛЕМА БЕЗОПАСНОСТИ: Утечка токена Telegram бота

## Проблема
Токен Telegram бота был закоммичен в публичный репозиторий GitHub.

**Токен:** `8351741227:AAEkDtvBaDe3HP_reSegjtaoiUfItqRhKJI`

## Немедленные действия

### 1. Отзовите токен и создайте новый

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/revoke` или `/revoke_token`
3. Выберите вашего бота из списка
4. BotFather отзовет старый токен и выдаст новый
5. **Сохраните новый токен в безопасном месте**

### 2. Обновите токен на сервере

На VPS сервере:

```bash
# Отредактируйте .env файл
cd /opt/hp-life-tracker
nano .env

# Найдите строку TELEGRAM_BOT_TOKEN и замените на новый токен:
TELEGRAM_BOT_TOKEN=ваш_новый_токен_здесь

# Сохраните файл (Ctrl+O, Enter, Ctrl+X)

# Перезапустите бота
sudo systemctl restart hp-life-tracker-bot

# Или если бот запущен через docker:
docker compose restart
```

### 3. Обновите токен локально (если используете)

```bash
# Windows PowerShell
$env:TELEGRAM_BOT_TOKEN="ваш_новый_токен_здесь"

# Linux/Mac
export TELEGRAM_BOT_TOKEN="ваш_новый_токен_здесь"
```

### 4. Проверьте, что токен удален из кода

Токен уже удален из `Backend/app/telegram_bot.py`. Теперь код использует только переменную окружения.

### 5. Закоммитьте исправление

```bash
git add Backend/app/telegram_bot.py
git commit -m "Security: Remove hardcoded Telegram bot token"
git push
```

**Важно:** Старый токен все еще виден в истории Git. Если репозиторий публичный, токен уже скомпрометирован. Обязательно отзовите его!

## Предотвращение в будущем

### ✅ Что уже сделано:
- Токен удален из кода
- `.gitignore` настроен для игнорирования `.env` и `config.py`
- Код теперь требует переменную окружения

### 📋 Чек-лист перед коммитом:
- [ ] Нет токенов, паролей или API ключей в коде
- [ ] Все секреты в `.env` файле (который в `.gitignore`)
- [ ] Проверен `git diff` перед коммитом
- [ ] Используется `git-secrets` или подобный инструмент (опционально)

## Проверка безопасности

Проверьте, что токен не используется:

```bash
# Поиск токена в коде (не должно быть результатов)
grep -r "8351741227" . --exclude-dir=.git

# Проверка .gitignore
cat .gitignore | grep -E "\.env|config\.py"
```

## Дополнительные меры безопасности

1. **Используйте GitHub Secrets** для CI/CD (если используется)
2. **Включите двухфакторную аутентификацию** на GitHub
3. **Регулярно ротируйте токены** (меняйте каждые 3-6 месяцев)
4. **Используйте инструменты для сканирования секретов:**
   - [git-secrets](https://github.com/awslabs/git-secrets)
   - [truffleHog](https://github.com/trufflesecurity/trufflehog)
   - [GitGuardian](https://www.gitguardian.com/)

## Если токен уже использован злоумышленниками

Если вы подозреваете, что токен был использован:

1. **Немедленно отзовите токен** через BotFather
2. **Проверьте логи бота** на подозрительную активность
3. **Измените пароли** всех связанных аккаунтов
4. **Проверьте настройки бота** в BotFather
5. **Создайте новый токен** и обновите его везде

---

**Дата обнаружения:** 2026-01-13  
**Статус:** Токен удален из кода, требуется отзыв через BotFather
